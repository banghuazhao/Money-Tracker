# macOS
.DS_Store

# Xcode user state
xcuserdata/
*.xcuserstate

# AdMob production config — contains real release ad unit IDs, keep out of git.
# Copy Config/Release.xcconfig.example to Config/Release.xcconfig and fill in.
Release.xcconfig

# Fastlane — not part of this project
fastlane/

# Xcode workspace shared data (scheme containers, etc.)
Money Tracker/Money Tracker.xcworkspace/xcshareddata/
